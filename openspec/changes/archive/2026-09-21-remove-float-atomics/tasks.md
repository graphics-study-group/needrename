## 1. SumByKey GPU algorithm

- [x] 1.1 Create `engine/Physics/shader/algorithm/sum_by_key.comp` implementing the recursive block reduction (256-entry blocks, shared-memory same-key doubling with dead marking, aliveness suffix scan, two boundary records per block with the all-zero whole-block record, final-level direct writes, `N`-channel loop with `MAX_CHANNELS=8`) and verify the `physics_shader` target compiles it to `sum_by_key.comp.spv` without glslang errors
- [x] 1.2 Implement `engine/Physics/gpu_algorithm/SumByKey.h/.cpp` following the house pattern (pImpl, `(DeviceContext&, max_entries, max_key_value, num_channels)` constructor with validation, deferred shader loading, `GetNumLevels`, `GetRequiredRecordsBytes`, `Record` dispatching k levels with internal barriers and per-level push constants) and verify the `EnginePhysics` target builds
- [x] 1.3 Add a headless GPU test program (mirroring the existing headless `DeviceInterface` usage) that runs `SumByKey` on host-uploaded sorted keys and reads back outputs, covering: many keys in one block, a single key spanning many blocks, and INVALID-sentinel entries; verify all per-key sums match expected values and keys >= max_key_value are untouched
- [x] 1.4 Extend the headless test to `num_channels = 7` with distinct per-channel values and verify all seven per-key sums are produced by a single `Record` call with the level count independent of the channel count

## 2. RadixSort primary-only mode

- [x] 2.1 Add a primary-key-only mode to `RadixSort` (`Record` variant or mode parameter) that runs 4 passes sorting by `.x` with `.y` riding along, reusing the existing histogram/prefix/scatter shaders and count-guard semantics, and verify the engine builds
- [x] 2.2 Extend the headless test to sort `(key, payload)` pairs in primary-only mode and verify keys ascend, every payload appears exactly once, and the result lands in the ping buffer

## 3. XPBD solver shaders

- [x] 3.1 Create `engine/Physics/shader/solver/XPBDSolver/invert_permutation.comp` that writes `pos_of[pairs.y] = p` and `sorted_keys[p] = pairs.x` per sorted position and verify it compiles to SPIR-V. *(superseded by 9.1–9.6: `SumByKey`'s level 0 now gathers values by the pair's own slot id, so no inversion exists — the shader and both arrays were deleted)*
- [x] 3.2 Rework `apply_body_position_deltas.comp` to read the three per-body partial sets (contact/hinge/fixed) plus their counts, average by the total count, apply, and zero the per-body count outputs unconditionally per body; apply the same change to `apply_body_velocity_deltas.comp` (single partial set); verify both compile
- [x] 3.3 *(superseded by 7.1–7.4)* Create `entry_layout`/`entry_validity` includes and a shared `xpbd_side_static_valid` predicate so entry-key writing and delta accumulation could not drift; both were built, and `xpbd_entry_validity.glsl` was later deleted — see 7.1
- [x] 3.4 *(superseded by 7.1–7.4)* Rework `accumulate_contact_position.comp` into `mode 0/1`… — the mode-based structure was replaced by separate entry-pass shaders — see 7.1–7.4
- [x] 3.5 *(superseded by 7.1–7.4)* Apply the same `mode 0/1` rework to `accumulate_contact_velocity.comp`, `accumulate_hinge_position.comp`, and `accumulate_fixed_position.comp` — superseded — see 7.1–7.4

## 4. XPBDGpuSolver pipeline and buffers

- [x] 4.1 Extend `XpbdGpuSolver::Impl` buffer inventory (entry pairs ping/pong, `pos_of`, `sorted_keys`, channel-major scratch, per-level records buffers, per-type `7 × body_count` output buffers, entry-count buffer) with sizing via the new static helpers, and verify the solver builds and allocates without errors. *(the `pos_of` and `sorted_keys` members were deleted by 9.5; the channel-major scratch is now one buffer per constraint type rather than two for contact)*
- [x] 4.2 Create the three `SumByKey` instances (contact/hinge/fixed) plus the `RadixSort` primary-only instance in `EnsureShadersLoaded`, and load `invert_permutation.comp` as a solver stage; verify the solver initializes. *(the `invert_permutation` stage was deleted by 9.4)*
- [x] 4.3 *(superseded by 7.5)* Rewire `GPUStep` around the `mode 0/1` structure — the pipeline is re-wired again for the entry-pass/accumulate split — see 7.5
- [x] 4.4 *(superseded by 7.5)* Implement hinge/fixed entry-list caching (rebuild only when joint/body counts change) — the cache was removed and all entry lists are rebuilt every substep — see 7.5
- [x] 4.5 Delete the now-unused `gpu_position_delta_count` / `gpu_velocity_delta_count` buffers from the pre-change solver and verify the solver still runs with correct physics

## 5. Device feature removal

- [x] 5.1 Remove `VK_EXT_SHADER_ATOMIC_FLOAT_EXTENSION_NAME` from `DEVICE_EXTENSION_NAMES`, the `shaderBufferFloat32AtomicAdd` requirement from `GetPhysicalDeviceSuitabilityScore`, and the `vk::PhysicalDeviceShaderAtomicFloatFeaturesEXT` node from the `CreateDevice` pNext chain in `engine/Rhi/Device/DeviceInterface.cpp`, and verify the engine builds
- [x] 5.2 Run the physics example on a device/configuration lacking `shaderBufferFloat32AtomicAdd` (or a driver setting disabling it) and verify device selection succeeds and the XPBD simulation behaves as before (box stack settles, hinge chain stays attached). *Best-effort note: the local RTX 3090 supports the feature, so physical-absence could not be exercised here; instead the code no longer queries/enables it, all solver shaders declare no `AtomicFloat32AddEXT`, and the physics tests pass after removal.*

## 6. Verification and cleanup

- [x] 6.1 Run `openspec validate "remove-float-atomics"` (the CLI equivalent of `openspec verify`) and resolve any validation findings
- [x] 6.2 Verify no solver shader declares `GL_EXT_shader_atomic_float` or emits `AtomicFloat32AddEXT` (checked with `spirv-dis` over all solver `.spv` files)
- [x] 6.3 Re-run the full build, the headless GPU tests (`gpu_sum_by_key_test`, `gpu_radix_primary_test`) and the physics integration tests after the section 7 restructure, and re-run `openspec validate` — full suite: 56/56 passed
- [x] 6.4 Re-verify with `spirv-dis` that no solver shader emits `AtomicFloat32AddEXT` after the section 7 restructure, and that no accumulate shader's push-constant block contains a mode/phase selector — all 18 solver shaders clean; no `mode` field anywhere

## 7. Restore the pre-change shader shape (entry passes split out; no modes)

The `mode 0 / mode 1` structure is replaced: entry-list construction becomes its own shader per constraint type, the accumulate shaders return to their pre-change text, and clearing becomes explicit. Rationale is in design.md Decisions 2, 8, 9 and 11.

- [x] 7.1 Create `engine/Physics/shader/solver/XPBDSolver/entries/contact_entries.comp`: dispatched over the contact entry capacity (one invocation per contact point, writing slots `2i` and `2i+1`), writing every slot it owns — key = owning body index, or `kInvalidKey` when the contact does not exist this substep or an owner index is out of range. It must not read body state (alive / kinematic / mass) and must not return before writing its slots. Compiles to SPIR-V; binds 5 storage buffers (CollisionIds, CollisionCount, ShapeBoundRigidBody, RigidBodyAlive for its length only, EntryPairs).
- [x] 7.2 Create `entries/hinge_entries.comp` and `entries/fixed_entries.comp`: dispatched over `4 × max(1, joint_count)` capacity (one invocation per joint slot group, writing 4 slots: 2 bodies × 2 scalar constraints), writing the owner body indices or `kInvalidKey` for a missing joint / out-of-range owner. No body-state reads, no early return before writing an owned slot. Both compile; each binds 3 storage buffers.
- [x] 7.3 Restore `accumulate_contact_position.comp` to its pre-change text: identical guards, control flow and math, `GL_EXT_shader_atomic_float` removed, the `mode` push-constant field removed, and each float `atomicAdd` accumulation block replaced by a single `scatter_slot(slot, lin, ang)` call; a non-contributing contact writes nothing. Compiles and declares no `AtomicFloat32AddEXT`.
- [x] 7.4 Apply the same restoration to `accumulate_hinge_position.comp`, `accumulate_fixed_position.comp` and `accumulate_contact_velocity.comp` (the velocity shader reuses the contact entry list), including deletion of the scaffolding that only existed for the `mode` branch. All compile without the float-atomic extension. Side fix: both joint shaders were invalid UTF-8 (an em dash written as `E2 80 3F`) and are now clean UTF-8. *(the velocity shader's separate scratch was removed by 9.5 — it now shares the contact position scratch)*
- [x] 7.5 Delete `common/xpbd_entry_validity.glsl`, remove `xpbd_side_static_valid` from every shader, and reduce `common/xpbd_entry_layout.glsl` to the channel conventions, `kInvalidKey` and the slot-numbering helpers (`contact_slot`, `joint_slot`) shared by the entry and accumulate shaders; add `common/xpbd_scatter.glsl` with the single `scatter_slot` writer. No shader includes the deleted file; the physics shader target compiles.
- [x] 7.6 Rewire `XPBDGpuSolver::GPUStep`: build → sort → invert for all three constraint types **every substep** (entry-build caching deleted; buffer/`RadixSort`/`SumByKey` recreation still keyed on count changes); per position iteration run `dispatch_clear(scratch)` → accumulate → `SumByKey::Record` → apply for each type; per velocity iteration run `dispatch_clear(velocity scratch)` → accumulate → `SumByKey::Record` → apply; clear the three per-body output buffers once before the position loop. Solver builds and the physics tests pass. *(the "→ invert" step and the separate velocity scratch were removed by 9.4–9.5; the rest of this wiring stands)*
- [x] 7.7 Verify the zero-joint case cannot read uninitialised entry pairs: with `hinge_joint_count == 0` (and likewise fixed) the capacity is clamped to one joint's worth and the entry pass is dispatched with at least one workgroup, writing `kInvalidKey` to all four slots before the sort runs. The contact pass and both joint passes also bound their slot index against `entry_capacity`, so the workgroup rounding cannot write past the entry list into the ping/pong buffers. `invert_permutation.comp` additionally drops (as `INVALID`) any pair whose slot id is out of range instead of performing a wild `pos_of` write. *(the inversion pass is gone as of 9.4; the equivalent protection now lives in `SumByKey`'s level-0 gather — 9.1 — which treats a payload slot outside `[0, input_count)` as an out-of-range element)*
- [x] 7.8 Confirm the split did not change solver semantics: `git diff 190eb97f -- <accumulate shaders>` now differs only in the extension line, the accumulator bindings, the push-constant block, comments and the write sites (`-24/+23`, `-26/+24`, `-34/+26`, `-30/+22` lines); the resting-contact, gravity, windowed and actuator physics scenarios all pass.

## 8. Resource lifetime: sizing belongs to `PreGPUStep`

An earlier revision sized the hinge/fixed reduce groups inside `GPUStep` (a leftover of the `mode` design, where the group's `Ensure` was welded to the entry-build dispatch decision) and tracked geometry with three `cached_*` counters in the solver. That violated the solver's documented lifecycle — `PreGPUSolver.h` reserves `PreGPUStep` for "buffer sizing, CPU uploads", and `ISolver.h` states `PreGPUStep` runs outside the command-buffer scope — and the counters were only maintained for the joint groups: the contact group was created with `if (!contact_sort)` / `if (!contact_sum)`, so when `max_contact_point` (which tracks `shape_count`) changed between frames the `RadixSort`/`SumByKey` instances kept a stale capacity while the buffers and `pair_count` were resized. That threw out of `RadixSort::Record` when the capacity grew and silently reduced with the wrong channel stride when it shrank.

- [x] 8.1 Move every capacity-dependent `Ensure` for the hinge and fixed groups (reduce-group buffers, per-type lagrange buffers, `RadixSort`/`SumByKey` instances, `pair_count` constant) from `GPUStep` into `PreGPUStep`, so `GPUStep` contains no allocation and no host-visible write and only derives dispatch geometry.
- [x] 8.2 Delete `cached_hinge_count`, `cached_fixed_count` and `cached_body_count`; add `Impl::EnsureSortAndSum(sort, sum, capacity, body_count)` which asks the objects (`RadixSort::GetMaxElemCount`, `SumByKey::GetMaxEntries`, `SumByKey::GetMaxKeyValue`) whether they still match and rebuilds them otherwise, and route all three constraint types through it. The solver no longer keeps a shadow copy of state its resources already know.
- [x] 8.3 Pin the contract in the headless algorithm tests: `gpu_radix_primary_test` asserts `GetMaxElemCount()` reports the construction value and that `Record` rejects an `elem_capacity` above it; `gpu_sum_by_key_test` asserts `GetMaxEntries`/`GetMaxKeyValue`/`GetNumChannels`/`GetNumLevels` report the construction geometry and that no shader is loaded before the first `Record`.
- [x] 8.4 Rebuild and re-run the full suite (56/56 passed) and `openspec validate`.
- [ ] 8.5 *Not covered*: the concrete trigger for the stale-capacity bug — the shape count changing between two frames of a live solver — has no end-to-end test, because `PhysicsApp` freezes the scene at `CommitScene` (`Add*` throws afterwards) and there is no headless `PhysicsScene` + `XpbdGpuSolver` harness in `test/`. Closing this needs a small harness that can `AllocateCollisionShapeSlot`/`SubmitCollisionShape`/`SyncGpuBuffers` between steps.

## 9. Replace the permutation inversion with a level-0 gather

`pos_of` + `sorted_keys` + `invert_permutation.comp` existed only because `SumByKey`'s level 0 insisted on a plain `uint[]` key array aligned with the value positions. Once level 0 may take the sorted `(key, slot)` pair array and gather each value by the slot that pair carries, the inversion is pure overhead: two `max_entries`-sized buffers per constraint type, one shader with its stage, binding and push block, one dispatch per substep, and a bijection invariant (`pos_of` must be written for *every* slot) that the entry passes had to uphold.

The scatter also gets *better*: `pos_of[slot] = p` is an inversion, so an accumulate dispatch wrote a permuted order, whereas writing at the slot index is an identity — for contacts `slot = cidx * 2 + side`, i.e. consecutive threads writing consecutive addresses. The permutation cost simply moves to level 0's gather, one extra indirection per read over exactly the bytes the reduce already touched.

This section also unifies the two contact scratch buffers, which is a separate but adjacent finding: the position and velocity phases are strictly sequential and each clears before its accumulate, so they can share one.

Scope: **no change to the reduction algorithm, the entry passes' contracts, Scheme A, the channel layout, the slot-numbering helpers, or the sort mode.** The sorted pair array stays exactly as it is; only what consumes it changes. Rationale: design.md Decision 6 (which this supersedes), plus Decisions 2, 4, 8, 9 and the Scheme A addendum.

- [x] 9.1 Add the `PairsIn` (`uvec2[]`) binding and a `gather_pairs` push-constant field to `engine/Physics/shader/algorithm/sum_by_key.comp`. At level 0 load the key from `pairs_in.v[e].x` and channel `c` from `values_in.v[c * input_count + pairs_in.v[e].y]`; for a payload slot outside `[0, input_count)` load 0.0 for every channel while keeping the pair's own key (rewriting the key would break the ascending-order requirement and could split a real key's run, losing a partial sum) instead of reading the value buffer out of range. Levels >= 1 keep loading `keys_in[e]` / `values_in[c * input_count + e]`. The shader still has one reduction body — only its load path is selected. It compiles to SPIR-V with `MAX_CHANNELS = 8` and the same reduction code as before.
- [x] 9.2 Change `SumByKey::Record` to take the sorted pair buffer in place of the key array — `Record(cb, pairs_in_buf, values_in_buf, records_buf, out_values_buf)` — grow `SumByKeyPush` from 16 to 20 bytes with `gather_pairs`, set the flag only for level 0, and bind `PairsIn` at every level (the caller's pair buffer; harmless where unread). Update the class doc comment: the per-level input shapes, the pair-array sizing (`max_entries` × `uvec2`), the out-of-range-slot rule, and the SSBO count (5 → 6). `EnginePhysics` builds.
- [x] 9.3 Rewrite `scatter_slot` in `common/xpbd_scatter.glsl` to write `scratch_values.v[c * capacity + slot]` directly, dropping its `pos_of` dependency and its requirement that the including shader declare a `PosOf` binding. Remove the `PosOf` binding from `accumulate_contact_position.comp`, `accumulate_contact_velocity.comp`, `accumulate_hinge_position.comp` and `accumulate_fixed_position.comp`, renumbering the `ScratchValues` bindings without leaving gaps. All four still compile and declare no float-atomic extension.
- [x] 9.4 Delete `engine/Physics/shader/solver/XPBDSolver/invert_permutation.comp`, its `ComputeStage`, its `ComputeResourceBinding`, `Impl::RecordInvert` and the `CapacityPush` push-constant struct. Confirm the slot-id bound it used to enforce is now covered by 9.1, and re-run the CMake configure so the physics SPIR-V target (a `GLOB_RECURSE`) drops the deleted shader and picks up nothing stale.
- [x] 9.5 Delete `gpu_{contact,hinge,fixed}_keys` and `gpu_{contact,hinge,fixed}_posof` from `Impl`, and share one contact scratch between the position and velocity phases: `gpu_contact_scratch_vel` is deleted and both phases bind `gpu_contact_scratch_pos` (a comment at each site records why it is safe). Update `EnsureReduceGroup`'s buffer list. Solver buffer inventory drops 27 → 20. *(implemented as: the six key/posof buffers and `gpu_contact_scratch_vel` are gone, and the shared buffer is renamed `gpu_contact_scratch` since it no longer belongs to one phase — inventory 27 → 20)*
- [x] 9.6 Update `common/xpbd_entry_layout.glsl` to describe the new conventions — scratch is channel-major with stride `entry_capacity` and is indexed by **slot**; `SumByKey`'s level-0 input is the sorted `(key, slot)` pair array; the per-body output is channel-major with stride `body_count` — and remove the `pos_of` / sorted-key-array text.
- [x] 9.7 Rebuild, re-run the full suite (headless + windowed + gpu) and `openspec validate`. Extend the headless coverage in `gpu_sum_by_key_test`: a gather-mode case whose payload slots appear out of order and whose input spans multiple blocks (so levels >= 1 still exercise the record path), asserting the per-key sums are independent of payload order and match the position-indexed result; and a case asserting that an entry whose payload slot is out of range is dropped (zero contribution, key left unchanged) rather than read. Confirm with `spirv-dis` that the reduce shader binds six storage buffers and that no solver shader references `pos_of`. The physics scenarios (resting contact, box stack, hinge chain) must be unaffected — in particular the velocity results, which now share the contact scratch. *(full suite 56/56 passed; `spirv-dis` shows `PairsIn`/`KeysIn`/`ValuesIn`/`RecKeys`/`RecValues`/`OutValues` (6 storage buffers) and a 20-byte push block with `gather_pairs` at offset 16; zero `PosOf`/`pos_of`/`invert_permutation` identifiers in any compiled shader, solver inventory 18 → 17 files)*
