## 1. The stable radix pass

- [x] 1.1 Add `engine/Physics/shader/algorithm/radix_block_histogram.comp`: per-block 256-bin histogram in shared memory over the block's digits, written transposed as `hist[digit * num_blocks + block]`. Predicate the whole body on `idx < elem_count` (no early `return` before a `barrier()`), contribute a sentinel digit for inactive invocations, and write every cell exactly once. Verify: the shader compiles to SPIR-V; `spirv-dis` shows no early return before a `Barrier` and no float atomics.
- [x] 1.2 Rewrite `engine/Physics/shader/algorithm/radix_scatter.comp`: `pos = scanned[digit * num_blocks + wg] + warp_offset + rank_in_warp`, with the rank computed deterministically in shared memory (digit snapshot + per-warp counts + a bounded comparison loop), the payload store predicated on a `has_payload` push-constant flag, and the payload binding satisfied with a placeholder when the caller has none. Verify: compiles; `spirv-dis` shows no `atomicAdd` on the output position path and no subgroup opcodes.
- [x] 1.3 Delete `engine/Physics/shader/algorithm/radix_histogram.comp` and `radix_prefix_sum_256.comp`. Verify: the shader build no longer produces their `.spv` files and nothing references them.
- [x] 1.4 Confirm the pass structure end to end on paper against the verified identity: transposed histogram → one flat exclusive `ParallelScan` over `256 * ceil(capacity/256)` elements → stable scatter, with no clear pass. Verify: a scratch record of the derivation exists in design.md D2 and the CPU brute-force check is reproduced by the new unit test.

## 2. RadixSort's contract

- [x] 2.1 In `engine/Physics/gpu_algorithm/RadixSort.h`: replace the `uvec2`-and-mode API with `RadixSortBuffers` (key arrays a/b, optional payload arrays a/b, scratch, count) and `RadixSortOutput` (keys, payload), and the `Record(cb, buffers, elem_capacity, max_key_value)` signature. Document the contract in the header: stability, the derived pass count, "every key below `2^(8 * num_passes)`", the returned result buffers, and the caller's obligation to assert its own key domain. Verify: the header compiles and `RadixSortMode`, `kNumPasses`, `kNumPrimaryPasses`, `kMaxShapeCount` and `max_shape_count` are gone.
- [x] 2.2 Replace the static sizing helpers with `GetRequiredScratchBytes(max_elem_capacity)` (transposed histogram + `ParallelScan::GetRequiredBlockSumsBytes`) and `GetRequiredTempBytes(max_elem_capacity)` (one word per element, per temp array). Verify: the values match the formulas for a capacity that is not a multiple of 256.
- [x] 2.3 In `RadixSort.cpp`: derive the pass count from the bound, own an internal `ParallelScan` (rebuild it when a call's capacity exceeds the built maximum), partition the caller's scratch into the histogram and the block-sums regions with offset `BindBuffer` calls, alternate the key/payload arrays per pass, return the buffers the last pass wrote, keep the per-call buffer-size validation, and no-op for a zero capacity or a zero pass count. Verify: `EnginePhysics` builds; the sort records 3 dispatches for a one-byte bound and 0 for `max_key_value <= 1`.

## 3. SumByKey's level-0 input

- [x] 3.1 In `engine/Physics/shader/algorithm/sum_by_key.comp`: replace the `PairsIn` (`uvec2[]`) binding with `PayloadIn` (`uint[]`), take the level-0 key from the existing `KeysIn` binding, gather channel `c` from `values_in[c * input_count + payload_in[e]]`, and rename the push-constant flag to `gather_payload`. Keep the entry-count semantics (an element at or beyond the count, or one whose payload index is, contributes 0.0 to every channel while keeping its key). Verify: compiles; `spirv-dis` shows seven storage buffers and the same 20-byte push block.
- [x] 3.2 In `SumByKey.cpp`: bind the payload array with the key-array element size, drop the placeholder binding that pointed `KeysIn` at the pair buffer in gather mode, and update the push-constant field name. Verify: `gpu_sum_by_key_test`'s gather scenarios pass after being converted to key-plus-payload input.

## 4. Solver adaptation

- [x] 4.1 In `engine/Physics/shader/solver/XPBDSolver/entries/{contact,hinge,fixed}_entries.comp`: write the key array and the payload array (the entry's own slot id) on every dispatch, and replace `kInvalidKey` with `body_count` as the invalid-slot key. State at the top of each file that the payload must be re-identified every substep because the sort permutes it in place. Verify: compiles; the entry count remains a prefix of the sorted order.
- [x] 4.2 In `common/xpbd_entry_layout.glsl`: update the sentinel constant and the layout comment (key array plus payload array; the sentinel is `body_count`, which is `SumByKey`'s `max_key_value` and is dropped by it). Verify: no shader still refers to `0xFFFFF` as the invalid key.
- [x] 4.3 In `XPBDGpuSolver.cpp`: allocate key and payload ping-pong arrays per reduce group, pass `max_key_value = body_count` to each `RecordSort` with a host assertion that the key domain fits, size the scratch with `RadixSort::GetRequiredScratchBytes(capacity)` (replacing the fixed 1 KB histogram buffer), and bind the sorted buffers that `RecordSort` returns into `RecordReduce`. Verify: `xpbd_reduce_capacity_test` passes and the solver records one sort pass per group for a body count below 256.

## 5. Broad phase adaptation

- [x] 5.1 In `generate_broad_pairs.comp` and `generate_global_pairs.comp`: write the packed key `a * shape_count + b` instead of the `uvec2` pair, keeping the canonical `a < b` order and the existing pair-count guard. Verify: the packed key is injective over the configured shape count.
- [x] 5.2 Add `engine/Physics/shader/collision/SpatialHashBroadDetector/unpack_pairs.comp`: read the compacted keys below the unique count, write `uvec2(key / shape_count, key % shape_count)` into the pair buffer, and publish `pair_count` from the unique count. Verify: compiles; the restored pair equals the pair that was packed and keeps `pair.x < pair.y`.
- [x] 5.3 In `SpatialHashBroadDetector.cpp`: allocate the key array and its temp (replacing the `uvec2` pair buffer as the sort's input), size the radix scratch from the capacity, sort with `max_key_value = shape_count * shape_count - 1` and no payload, run `CompactUnique` on the keys, record `unpack_pairs` and drop the separate `DispatchCopy`, and assert `shape_count <= 65536` in `Configure`. Keep the fallback path and the narrow phase's `uvec2` contract unchanged. Verify: the dedup scenario in the new test passes and the detector's output buffers are still `uvec2` pairs with a correct count.

## 6. Lagrange multiplier clears

- [x] 6.1 In `XPBDGpuSolver.cpp`: load `solver/XPBDSolver/clear_hinge_lagrange.comp.spv` and `clear_fixed_lagrange.comp.spv` as `ComputeStage`s and dispatch both once per substep before the position iteration loop, with `hinge_joint_count` and `fixed_joint_count` as their push constants, binding both buffers of each shader. Remove the integer-clear workaround for `gpu_hinge_axis_lagrange`. Verify: the substep records five multiplier clears (contact, hinge axis, hinge anchor, fixed rotation, fixed position) and a scene with hinge and fixed joints passes its existing tests.

## 7. Tests

- [x] 7.1 Rewrite `test/engine/headless/gpu_radix_multiblock_test.cpp` for the new contract: ascending output, **stability** (equal keys keep their input order, over a multi-block capacity), payload bijection, a keys-only sort, a one-pass bound and a multi-pass bound, the returned result buffer matching the last pass, a count smaller than the capacity with tail blocks beyond it, and a zero capacity. Verify: the test is green, and it fails if the scatter's rank term is removed.
- [x] 7.2 Add `test/engine/headless/gpu_compact_unique_test.cpp` (registered in `test/engine/headless/CMakeLists.txt`): all-duplicate, partly-duplicate, unique, single-element and zero-element inputs; a size spanning more than one workgroup; canonical pair order preserved after unpacking; `pair_count` equal to the unique count; and a pack-sort-unpack round trip equal to the original pairs. Verify: the test is green and it fails against the pre-change scatter.
- [x] 7.3 Register the existing `test/app/physics/physics_app_stability_test.cpp` in `test/app/physics/CMakeLists.txt` and remove or fold `gpu_radix_primary_test.cpp` (its cases are covered by 7.1). Verify: the stability probe fails against the pre-change code and passes after this change.
- [x] 7.4 Convert `test/engine/headless/gpu_sum_by_key_test.cpp`'s gather scenarios to the key-plus-payload input and keep the entry-count, INVALID-sentinel and out-of-range-payload cases. Verify: the whole headless suite is green.

## 8. Verification and close-out

- [x] 8.1 Run the physics test suite (`ctest --preset msvc-debug` or the physics subset) and confirm the 6-layer brick scene survives the 60-step probe with max |v| comparable to the pre-change float-atomic solver. Verify: the probe reports no anomaly and the windowed scene no longer blows up.
- [ ] 8.2 Run `openspec validate stable-radix-sort-soa` and resolve the MODIFIED-block collisions with `remove-float-atomics` and `xpbd-entry-count-driven-reduce` (archive them first, or drop their superseded `gpu-radix-sort`/`gpu-sum-by-key` deltas). Verify: validation passes with no requirement-header conflicts.
- [x] 8.3 Record the deliberately unfixed issues (pair-count overflow, shape-slot high-water mark, `max_global_shape_count` truncation, `ComputeStage`/binding lifetime, `CompactUnique`'s remaining `collision/` shader dependency) outside this change. Verify: the list is handed over and this change's diff contains no drive-by fix for them.
